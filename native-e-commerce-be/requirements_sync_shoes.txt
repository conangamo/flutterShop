# Requirements for sync_8_shoes_ai.py
# Lightweight local AI embedding generation for shoes

# Database
psycopg2-binary==2.9.9

# Local AI/ML - Sentence Transformers
sentence-transformers==2.3.1
torch==2.1.2
transformers==4.36.2

# Image processing (optional, for future image-based embeddings)
Pillow==10.2.0
requests==2.31.0

# Note: First run will download ~80MB model (all-MiniLM-L6-v2)
# Subsequent runs use cached model - no internet needed!
